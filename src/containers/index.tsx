import React, { Fragment } from 'react';
import App from './ASFSAF/asdaSFD';

const RootView: React.StatelessComponent<{}> = () => (
  <Fragment>
    <App />
  </Fragment>
);

export default RootView;
